{
  "name": "axios-token-manager",
  "version": "0.1.0",
  "repository": "https://github.com/mickeypuri/axios-token-manager",
  "author": "Mickey Puri",
  "license": "MIT",
  "scripts": {
    "compile": "rimraf dist/lib && tsc && tsc --build tsconfig.es5.json"
  },
  "devDependencies": {
    "rimraf": "^4.3.0",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "axios": "^1.3.4"
  }
}
